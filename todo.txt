ROADMAP

current goals v0.8.0 (predicate logic update titled: "the big one"):
- add predicate logic
    X replace variables with predicates
        X make predicates
        X make sentences
    - create Context/Universe struct to hold truth values and constants.
##TEST##
    - replace Node::Variable with Node::Sentence
    - change shunting-yard to accomodate predicates
##TEST##
    - replace ExpressionVar with ExpressionPredicate (Do we even need them anymore now that we have predicates? maybe just change predicates to work )
##TEST##
    - add Operator::Universal and Operator::Existential
##TEST##
    - add ExpressionTree::universal() and ExpressionTree::existential()
    - add ExpressionTree/Node::quant_exch()
##TEST##
    - update README.md

design considerations:
- Sentence (not fully descriptive) vs AtomicSentence (Kinda long) vs ASentence (sounds Async) vs AtomSen(tence) (kinda confusing because it's so short)?

later goals:
v0.8.1 (speedup update, titled: "from tortoise to hare"):
- random expression generator (do this first for performance benchmarking)
- JIT compiler

v0.9.0 (immutable trees update titled: "hands off that tree!"):
- make immutable expression trees
    - rework all of the '_with' functions to use immutable trees to save on space.

v1.0 (full release update: "let's be clawgical about this"):
- upgrade documentation
    - add example code to all functions
    - add time and space complexities of all functions
- get 100% test coverage
- benchmarks
- reasonable optimizations

whenever (unimportant and complicated):
- .comm_eq() (tells whether two expressions are commutatively equivalent) maybe impossible.
- maybe make Constant/Variable type so we dont have to constantly check if the constant name is valid.
 leave in string versions of functions, though. let the user pick their poison.

- optimize
- OPTIMIZE
- OPTIMIZE!!!